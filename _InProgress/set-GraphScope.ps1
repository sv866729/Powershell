
funtion
param(commands as array)
get the scope for each command and append to a array
set get only unique ones to delete repeat scopes
and set the sciope for the graph sessions
note that this isnt a secure method and shouldnt be used on a large scale